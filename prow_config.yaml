# This file configures the workflows to trigger in our Prow jobs.
# see kubeflow/testing/py/run_e2e_workflow.py
workflows:
  - app_dir: kubeflow/examples/test/workflows
    component: workflows
    name: examples-e2e
    job_types:
    - presubmit
  # The postsubmit run publishes the docker images to gcr.io/kubeflow-examples
  - app_dir: kubeflow/examples/test/workflows
    component: pytorch-mnist-webui-release
    name: nmist-webui
    job_types:
    # - postsubmit
    - presubmit # testing job while PR is WIP
    params:
        registry: "gcr.io/kubeflow-examples"
    include_dirs:
    - pytorch_mnist/web-ui/*

  - app_dir: kubeflow/examples/test/workflows
    component: pytorch-mnist-cpu-release
    name: nmist-cpu
    job_types:
    # - postsubmit
    - presubmit # testing job while PR is WIP
    params:
        registry: "gcr.io/kubeflow-examples"
    include_dirs:
    - pytorch_mnist/training/ddp/mnist/cpu/*

  - app_dir: kubeflow/examples/test/workflows
    component: pytorch-mnist-gpu-release
    name: nmist-gpu
    job_types:
    # - postsubmit
    - presubmit # testing job while PR is WIP
    params:
        registry: "gcr.io/kubeflow-examples"
    include_dirs:
    - pytorch_mnist/training/ddp/mnist/gpu/*

  - app_dir: kubeflow/examples/test/workflows
    component: pytorch-mnist-serve-release
    name: nmist-serve
    job_types:
    # - postsubmit
    - presubmit # testing job while PR is WIP
    params:
        registry: "gcr.io/kubeflow-examples"
    include_dirs:
    - pytorch_mnist/serving/*
