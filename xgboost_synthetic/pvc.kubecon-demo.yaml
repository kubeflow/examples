apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kubecon-demo
  namespace: kubeflow
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: standard